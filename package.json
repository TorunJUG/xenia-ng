{
  "name": "Xenia",
  "version": "2.0.0-SNAPSHOT",
  "description": "",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "karma start --single-run --browsers Chrome app/tests/karma.conf.js",
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 8000 -c-1"
  },
  "devDependencies": {
    "karma": "~0.12",
    "karma-chrome-launcher": "~0.1",
    "karma-cli": "0.0.4",
    "karma-jasmine": "^0.2.2",
    "karma-ng-html2js-preprocessor": "^0.1.2",
    "bower": "^1.3.1",
    "http-server": "^0.9.0"
  }
}